#!/usr/bin/env perl

use utf8;

+{
    connect => {
        uri      => q{} || $ENV{BUGZILLA_DASHBOARD_URI},
        user     => q{} || $ENV{BUGZILLA_DASHBOARD_USER},
        password => q{} || $ENV{BUGZILLA_DASHBOARD_PASSWORD},
    },
    recent_comments_limit    => 30,
    recent_attachments_limit => 30,
    comments_string_length   => 100,
};
